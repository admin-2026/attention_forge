steps:
    # load usr messages
    - type: user_input
      source: stdin|file
      output_data_key: user_message
    # chat with LLM
    - type: chat
      role_name: assistant
      input_data_key: user_message
      # output_data_key: chat_response

